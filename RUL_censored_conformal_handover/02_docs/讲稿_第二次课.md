# 讲稿：第二次课，从想法到能跑的东西

配套 PPT `第二次课_从想法到能跑的东西.pptx` 共 22 页。逐页对应。第 16 到 21 页是在真实 FD001 上跑出来的结果，数字都来自 `03_code/results/` 与 `03_code/logs/`。配套代码在 `03_code/`，讲第 9 到 14 页时把 `cmapss_common.py` 与 `s03_censoring.py` 开在旁边。

标记说明：

- 【念】可以基本照着念
- 【解释】必须停下来解释的词
- 【承上启下 · 必须说】逻辑扣子，漏掉听众就掉线
- 【停顿】停两三秒，或抛一个问题
- 【对照代码】指到脚本里的哪个函数或哪一行
- 【别讲】容易跑偏、不要展开的地方

全场约 75 分钟。前 25 分钟改方案（第 1 到 8 页），中间 30 分钟讲代码（第 9 到 15 页），最后 20 分钟讲结果（第 16 到 22 页）。

---

## 第 1 页　封面

【念】上次课讲完方案，我回去把数字算了一遍，有三处经不起核算。今天先把这三处改掉，然后把数据读进来，跑通第一版下界。

【念】今天的成果物不是新理论，是五个能跑的脚本。讲完每个人回去跑，把输出贴回来。

【念】数据还是 C-MAPSS，只用 FD001，最简单的子集。删失是人工造的，这句话今天会反复说，论文里每张图注都要写。

【停顿】老师先认错，学生才敢质疑。方案错了不丢人，算过之后不改才丢人。

---

## 第 2 页　今天的路线

【念】三段。第一段改方案，第 3 到 8 页。第二段跑代码，第 9 到 14 页。第三段交付，脚本、图、真实数据上的结果、作业。

【念】第二段要对着脚本听。每一页我都会说这一页对应脚本里哪个函数。

【念】一句话记住今天：先算再说，先跑通再谈理论。写方案之前把数字算出来，写理论之前把实验跑出来。

【别讲】不要在这一页预告结果。结果在第 16 页之后再讲，先让学生自己猜。

---

## 第 3 页　上次方案里三处经不起核算的地方

【承上启下 · 必须说】上次的方案有三个地方，我说的时候没有算过数，回去一算就站不住。三条各讲一分钟，重点不是错在哪，是"核算"这个动作。

【念】第一，样本量没算。FD001 只有 100 台发动机。上次说按发动机整机截取，删失 60% 再切校准集，校准折可能只剩十几台。这个数当时没算就往下写了。改法：选择改在样本层面做，删失机的早期循环照样能用；把样本量核算写进脚本 s03，先算再说。

【念】第二，权重相乘没有保证。删失权重有似然比含义，时序权重是启发式的，两个的保证形式完全不同，相乘之后还剩什么保证，我说不清。改法：不相乘。同机相关改用两层结构处理，全部循环校准和一机一点校准两版都跑，先看实验。

【念】第三，成本参数是编的。拆检成本、非计划停场成本，我一个真实数字都没有。编一组参数算曲线，等于自己定输赢。改法：不做成本曲线，改报五个统计指标。

【对照代码】第二条引出两个代号：Cw 是全部循环校准，Cw1 是一机一点校准。后面每页都会出现，先记住。

【停顿】三条的共同点：都是"没算就写"。今天之后，方案里每一个数都要能指到脚本的哪一行算出来的。

---

## 第 4 页　修正一：删失在剩余寿命里的正确形态

【承上启下 · 必须说】上次说的"整机截取"错在把删失的粒度和选择的粒度混在一起了。下线是整台机的事，但要不要用一个样本，是逐样本的事。

【念】规则一行：保留样本 (i, t)，当且仅当 C_i − t ≥ c0。C_i 是第 i 台机的下线时刻，t 是当前循环，c0 是我们定的观测视野。对保留下来的样本，min(RUL, c0) 一定看得见。

【解释】为什么丢的是最后 c0 个循环。在下线前最后 c0 个循环上，这台机会不会在 c0 之内坏掉，我们看不到，min(RUL, c0) 算不出来。再往前的循环不受影响：要么它在 c0 之内坏了，我们看到了失效；要么它撑过了 c0，我们也看到了。

【念】看右边的例子。一台删失机，F = 200，C = 150，c0 = 30。t 从 1 到 120 保留，共 120 个样本；121 到 150 观测到了但要丢，30 个；151 到 200 下线之后，本来就看不到，50 个。

【停顿】让学生自己算这三个数：120、30、50。算对了才往下走。

【念】对照一台失效机，F = 200，C = 210。同样的规则，保留 t ≤ 180，最后 20 个循环也要丢。它的标签在 t > 170 时是 F − t，小于 30；在 t ≤ 170 时是 30。

【承上启下 · 必须说】规则对失效机也执行，这一句很重要。不执行，保留下来的样本就不只是"协变量分布不同"，而是"哪类机被保留的规则不同"，后面的加权修正就没有依据。

【对照代码】`s03_censoring.py` 里 `keep = obs & ((C_row - tr.cycle.values) >= c0)`。下一行 `y_sel` 用可观测量算标签，再用 `assert` 核对它等于真值 min(RUL, c0)。断言触发就说明这一页讲错了。

---

## 第 5 页　修正一的副产品：一个真正属于我们的发现

【承上启下 · 必须说】这一页是修正一带出来的，也是今天最值钱的一页。它会写进论文引言。

【念】问：一个样本被保留的概率是多少？按规则，就是 C_i ≥ t + c0 的概率，也就是下线时刻生存函数在 t + c0 处的值，S_C(t + c0)。

【念】注意这个概率随 t 单调下降。即使下线时刻 C 和发动机自身完全无关，越老的循环，剩余观测窗越短，越容易被规则丢掉。

【解释】用右边的示意表说。t = 50 时 S_C 是 0.95，几乎都留下；t = 260 时只剩 0.10，十个里留一个。留下来的那一个要顶十个人的份，所以权重是 1 / S_C(t + c0)。表里数字是示意，真数字 s03 会打印 t = 50 到 250 处的权重，F05 画整条曲线。

【念】于是论文一里那句"完全独立删失就不用加权"，在剩余寿命问题里不成立。这是我们和医疗场景的第一个实质差别。医疗里一个病人一个样本，删失独立就是独立；这里一台机几百个样本，机级独立的删失到了样本级就依赖 t。

【念】三件事跟着这个发现走。第一，t 必须作为协变量进模型，特征表第一列就是 t。第二，S_C 可以从训练折发动机的下线时刻直接估计，因为 Type-I 设定下下线时刻每台机都记录，用经验分布就行，不需要 Kaplan-Meier。第三，脚本里 Cu 和 Cw 只差这一个权重，两行的差就是这个发现的直接证据。

【对照代码】`survival_C` 返回经验生存函数；`weight_fn(t) = 1 / max(S(t + c0), 1 / 训练折台数)`。分母设下限是防止极端权重，见第 11 页。

【停顿】让学生记住一句话：完全随机的下线，在样本层面不是完全随机的删失。

---

## 第 6 页　修正二：同机相关不再靠权重相乘

【念】上次的做法为什么不行。删失权重是似然比，来自分布偏移的精确推导。时序权重是"近的样本更像"的启发式，它对应的理论是牺牲一点覆盖率换稳健性，保证形式完全不同。两个相乘，得到的既不是似然比，也不在任何一个定理的覆盖范围内。

【念】正确的框架：发动机之间可交换，发动机内部的循环相关。这叫两层分层数据，共形预测在这类数据上已有专门结果，不需要自己发明。

【念】两个方案，今天都跑。Cw1，一机一点：每台校准发动机随机抽一个保留循环。校准点严格可交换，覆盖率保证是精确的。代价是校准折 40 台机分完只剩几十个点，分位数很粗，下界偏保守。脚本重复 20 次报均值。

【念】Cw，全部循环：所有保留循环都进校准集。样本多、下界紧，但同机相关让有效样本量远小于名义数。覆盖率还成不成立，靠实验看，而不是靠推。

【承上启下 · 必须说】Cw1 是安全的，Cw 是实用的。论文里两个都报，差距本身就是结果。

【念】把两层结构与删失一起处理并给出保证，是论文方法部分要做的理论工作。文献是 Dunn、Wasserman、Ramdas 2023 年 JASA 那篇，和 Lee、Barber、Willett 2023 年 arXiv 那篇。引用前按标题核对。

【对照代码】s02 里无删失版本也有这两版，`method_code` 分别是 `all` 和 `one_mean`，先在无删失时看两者差多少，再看删失后。s03 里 Cw1 那段用 `groupby("unit").sample(n=1)` 抽点，循环 20 次。

【别讲】不要展开两层结构的理论。今天只说"两版都跑，看差距"。

---

## 第 7 页　修正三：不编成本参数

【念】去掉维修触发规则加总成本曲线。拆检成本、停场成本没有真实来源，编出来审稿人一问就穿。以后拿到真实成本再做；拿不到，最多做成本比例的敏感性分析，并明说是敏感性。

【承上启下 · 必须说】有人会问，去掉成本曲线之后，下界上限是 c0，那还有什么用。回答：这不是缺陷。方法只回答一个问题，这台机能不能安全飞过接下来 c0 个循环。维修决策问的正是这个，c0 就是决策视野。脚本跑 20、30、50 三档。

【念】改报五个指标，s03 结果表每行都有。总体覆盖率 coverage，P(真实 RUL ≥ 下界)，应不低于 90%。决策区覆盖率 cov_dec，只看真实 RUL 小于 c0 的样本，这一段报错才出事，其余段下界天然满足，是最要紧的数。分段覆盖率 cov[a,b)，按真实 RUL 分四段各报一个。平均下界 mean_L，衡量紧不紧，永远说"至少 0 个循环"覆盖率也是 100%，所以必须一起看。无信息界比例 trivial，下界为负无穷的样本占比，校准集太小或权重太极端时会出现。

【解释】为什么要单独报决策区覆盖率。下界被 c0 封顶之后，RUL 大于 c0 的样本覆盖是平凡的，总体覆盖率会被这些样本稀释。跑出来很可能是总体 0.99、决策区远低于 0.9。这不是 bug，这是论文一 Theorem 5 说的条件覆盖不可能在我们这套设定下的具体形态，也是论文二自己承认的问题。下次课的 Mondrian 校准就是冲它去的。

【对照代码】五个指标都在 `cmapss_common.summarize_lpb` 里算，列名和 `s03_methods.csv` 一致。

---

## 第 8 页　样本量：先算再说

【念】FD001 的真实数字。训练集 100 台发动机，全部跑到失效；20631 行样本，每台平均约 206 个循环；循环数从 128 到 362，最短的机只有 128 个循环。测试集另有 100 台 13096 行，测试机在失效前被截断。

【念】核算公式两行。保留样本数是对每台机取 max(0, min(O_i, C_i − c0)) 再求和，O_i = min(F_i, C_i) 是实际观测到的循环数。保留发动机数是 C_i − c0 ≥ 1 的台数。

【念】三台机的手算例子：F=200、C=150，min(150, 120) = 120；F=200、C=260，min(200, 230) = 200；F=200、C=210，min(200, 180) = 180。

【念】两条底线。第一，校准折保留发动机少于 30 台，一机一点方案 Cw1 不做。90% 分位数在二十几个数上取没有意义。汇报时把低于 30 的组合标出来。第二，完整的核算表由 s03 打印，三档删失率乘四档 c0。表出来之前，不定删失率的上限。60% 那一档很可能要放弃，放弃就放弃。

【对照代码】s03 的 `[A]` 核算表，写到 `s03_accounting.csv`，F04 画成图。

【停顿】这是作业二。

---

## 第 9 页　数据长什么样：C-MAPSS 文件格式

【承上启下 · 必须说】从这一页开始对着脚本讲。先把数据格式讲清楚，s01 打印的每一个数都能对上。

【念】26 列，空格分隔，没有表头。第 1 列发动机编号，分组键，划分训练与校准按它来，RUL 文件第 k 行对应测试机 k。第 2 列循环序号 t，作为协变量进模型，修正一的权重也用它。第 3 到 5 列三个工况设置，FD001 单工况，带小噪声，本次不用。第 6 到 26 列 21 个传感器，去掉 1、5、6、10、16、18、19 七个近零方差的，剩 14 个。

【解释】s01 的两个易错点。工况设置带噪声，op1 在 ±0.009 之间晃，直接四舍五入到两位小数会数出三种组合，脚本按标称间隔取整后再数，FD001 应该是 1，FD002 和 FD004 是 6。传感器 6 只在 21.60 和 21.61 两个值之间跳，标准差约 0.001，所以近零方差判据用 0.01 而不是 0.001。

【念】标签与划分。训练集每台机跑到失效，RUL = F − t，封顶 125，这是论文二的做法。测试集每台机在失效前被截断，RUL 文件给最后一刻的真实 RUL，往前推得每个循环的 RUL。划分按发动机 60 / 40 分训练折与校准折，种子 2026。比论文二的 90 / 10 多留校准，因为一机一点方案需要足够多的发动机。

【念】特征 43 维：14 个传感器的当前值、最近 30 个循环的滚动均值、滚动标准差，加循环序号 t。滚动量只用过去的数据，不会泄漏未来；窗口不足 30 时用已有的算。

【念】点预测器用梯度提升树，300 轮，CPU 几秒训完，不需要显卡。这一步不是重点，够用就行，不调参。

【停顿】特意说一句：学生最容易在这一步花两周调网络，那是上一个方向的惯性。点预测器的精度不是这篇论文的贡献。

【对照代码】`load_subset`、`add_labels_train`、`add_labels_test`、`build_features`、`split_engines`、`fit_model`，都在 `cmapss_common.py`。

---

## 第 10 页　今天要跑通的流程

【念】六步。读数据，造特征与标签，按发动机划分，点预测，共形校准，评估。s02 对应无删失版本，s03 对应删失版本。第五步是唯一有统计内容的一步，其余都是数据工程。

【念】s03 在第五步前多两个动作。第一，造删失。给每台训练机抽一个下线时刻 C_i，服从 Uniform(60, U) 取整，与该机寿命 F_i 无关。U 用二分法调到 mean(C_i < F_i) 命中目标删失率 20%、40%、60%。下线时刻对每台机都记录，对应论文一的 Type-I 设定，现实含义是计划性退役日期事先知道。种子是 2026 加删失率乘 100，结果可复现。

【解释】"与寿命无关"是本次的假设，下次课换成依赖工况的删失。"每台机都记录"是 Type-I 的含义，所以 S_C 直接用经验分布。

【念】第二，选样本加权。按修正一的规则保留 C_i − t ≥ c0 的样本；标签 y = min(RUL, c0) 在保留样本上可观测；权重 1 / S_C(t + c0)，S_C 用训练折全部发动机的下线时刻估计，分母下限 1 / 训练折台数。

【念】两道保险。脚本用 assert 核对保留样本上按可观测量算出的标签等于真值，不等就停。每个删失率开头打印目标与实际删失率，相差超过 0.03 也要停。

【对照代码】`gen_censoring`、`survival_C`、`run_methods` 里的 `keep` 和 `weight_fn`，都在 `s03_censoring.py`。

---

## 第 11 页　下界怎么算，一页够

【承上启下 · 必须说】三行公式对应 `weighted_lpb` 里的三段代码。不推导，对着代码看。

【念】第一行，得分。V_j = m̂(x_j) − y_j，y_j = min(RUL_j, c0)。预测值减真值，预测偏高得分为正，这正是我们要防的方向。代码就是 `V = yhat_cal − y_cal`。

【念】第二行，临界值。η(x) 是加权经验分位数，p_j 正比于校准点权重 w_j，p_∞ 正比于测试点自己的权重 w(x)。代码三步：把校准得分排序、权重累加；阈值是 (1 − α) 乘以权重总和加测试点权重；用 searchsorted 找第一个累计权重达到阈值的位置，那里的得分就是 η。越界则 η 是无穷。

【念】第三行，下界。L(x) = m̂(x) − η(x)，再与 c0 取较小者。代码 `L = yhat_test − η`，`L = minimum(L, cap)`。

【解释】无删失时权重全为 1，第二行退化成普通的分裂共形，取第 ⌈(n+1)(1−α)⌉ 小的得分。所以 s02 和 s03 用的是同一个函数。

【念】权重极端时 p_∞ 会接近 1，η 变成无穷，下界变成负无穷。脚本对 S_C 设了下限截断，并单独统计这类无信息界的比例 trivial。

【念】两侧区间，论文二那种，另在 `split_conformal_twosided` 里，得分 |y − m̂|，只在 s02 里用来确认复现无误，覆盖率应接近 0.9。

【停顿】问学生：为什么下界只减 η 不加 η？答：维修只怕提前坏，只有下界进决策。这是第一次课的定义。

---

## 第 12 页　六条对比线，代号与代码一致

【念】上次讲稿说五条线，代码里是六条，多了 Cw1。代号和 `s03_methods.csv` 的 method_code 列一致。

【念】E，无删失参照。完整数据，标签 min(RUL, c0)。上界参照，看删失造成的损失有多大。E 不依赖删失率，脚本按 c0 缓存，三档删失率共用。

【念】A，丢弃删失机。只用跑到失效的发动机。常见错误做法一，样本变少，且分布有偏。删失率 60% 时校准折失效机可能很少，脚本照跑，那一行不可信。

【念】B，把下线当失效。观测到的全部行，标签 min(O_i − t, c0)。常见错误做法二，标签系统性偏低。

【念】Cu，选样本不加权。保留 C_i − t ≥ c0 的样本，权重 1。消融，不修正 t 引起的偏移。

【念】Cw，选样本加权。同上，权重 1 / S_C(t + c0)，全部保留循环校准。现行方法。

【念】Cw1，选样本加权，一机一点。每台校准机抽一个循环，重复 20 次取均值。校准点严格可交换的版本。

【承上启下 · 必须说】两组对照各说明一件事。Cu 对 Cw，只差权重，差就是第 5 页那个发现的证据。Cw 对 Cw1，只差校准点是全部循环还是一机一点，差就是第 6 页两个方案的代价。

【念】读表先看 cov_dec，再看 mean_L，最后看 trivial。E 是天花板，A 和 B 应该明显差，Cw 应该接近 E。不符合预期，改的是解释，不是数据。

【停顿】这是作业三。

---

## 第 13 页　评估协议

【念】用测试集的全部循环，不只用最后一点。论文二只用每台测试机的最后一个循环，FD001 一共 100 个点，分段之后每段十几个，没法看。测试机每个循环的真实 RUL 都能从 RUL 文件往前推，全部用上有 13096 个点。

【承上启下 · 必须说】但要说清楚一件事。13096 个点来自 100 台机，同机高度相关。覆盖率的点估计没问题，但它的不确定性要按 100 台机算，不能按 13096 算。论文里写清楚这一句，审稿人就不会拿它做文章。

【念】另一件事：测试机是在失效前随机截断的，靠近失效的点比训练集少，决策区的点数 n_dec 会明显少于总数，表里单独给了。

【念】按真实 RUL 分四段：[0, 15) 临近失效，[15, 30) 短，[30, 60) 中，[60, 125] 长。c0 = 30 时前两段是决策区，最要紧；后两段下界被封顶，覆盖趋于平凡。分段边界跟着 c0 走，c0 换成 50 决策区就是前三段，表里用 cov_dec 直接给。

【念】脚本同时打印总体覆盖率和决策区覆盖率，两个数一起看。总体达标而决策区不达标，就是论文二自己承认的那个问题。

【别讲】有学生会问为什么不按发动机做 bootstrap 算置信区间。回答：下次课做，这次先把点估计跑出来。

---

## 第 14 页　五个脚本，按顺序跑

【念】只改脚本顶部的 DATA_DIR，或者把数据目录作为命令行参数传进去。每一个的输出都要贴回来。

【念】s00_env.py，环境自检。打印 Python 与 numpy、pandas、scikit-learn 版本，Rscript 是否在 PATH，宋体与 Times New Roman 字体文件是否存在，FD001 三个文件是否齐全。验收：三个包有版本号，Rscript 找得到，两个字体都"有"。

【念】s01_inspect.py，探查结构。打印每个文件的形状、前五行、发动机台数、每台循环数、各列标准差、工况组合数，不建模。验收：26 列无 NaN；训练 100 台 20631 行；近零方差传感器 1、5、6、10、16、18、19；FD001 工况组合 1。

【念】s02_baseline.py，无删失基线。跑通六步流程，先算两侧区间确认复现无误，再算一侧下界两版。验收：两侧区间覆盖率 0.87 到 0.93；两行下界覆盖率不明显低于 0.85；results/ 出现 4 个 s02 文件。

【念】s03_censoring.py，删失版本。先打印核算表，再对三档删失率乘三档 c0 跑六条线，约几分钟。验收：实际删失率与目标差不超过 0.03；核算表记录校准折保留发动机数；断言不触发。

【念】s04_figures.R，出图。读 results/ 的 CSV 出 11 张图，宋体五号，Times New Roman 五号，PNG 与 PDF 各一份。验收：22 个文件；日志无"回退到系统字体"；F07 到 F09 目视无乱码。

【承上启下 · 必须说】顺序不能乱。s01 不贴回来，s02 跑出问题分不清是数据还是代码。跑不通贴报错，不要自己改代码。

【念】汇报格式在移交文档第 9 节：控制台输出原样贴，不总结成"覆盖率良好"。

---

## 第 15 页　跑完会得到 11 张图

【念】s04 生成，只出这 11 张，不自行加图。F01 失效循环数分布，F02 传感器退化轨迹，数据描述用。F03 删失时间线，第 4 页的图用真数据画。F04 样本量核算随 c0，第 8 页的表画成图。F05 权重随 t，第 5 页的表画成图。F06 基线分段覆盖，第 6 页两方案在无删失时的差。

【念】橙色三张是论文实验节的主结果图。F07 总体与决策区覆盖率随删失率；F08 分段覆盖率柱状图，各方法；F09 决策区覆盖率对平均下界，覆盖率与紧度的取舍。F10 一台测试机的真实 RUL 与各方法下界，最直观的一张。F11 两侧区间排序图，复现论文二的图。

【念】重点删失率与 c0 在脚本顶部的 FOCUS_RATE 和 FOCUS_C0 改，默认 0.40 和 30。所有图注都要写明删失是人工构造的。

---

## 第 16 页　真实数据跑出来了：验收对照

【承上启下 · 必须说】前面 15 页讲的是"怎么跑"。从这一页开始是"跑出来了什么"。先对验收，再看结果。

【念】环境：Python 3.11，numpy 2.4，pandas 3.0，scikit-learn 1.9，R 4.3.3。五个脚本全部退出码 0。

【念】s01 四条：训练集 100 台、20631 行、循环 128 到 362；测试集 100 台 13096 行，RUL 文件 100 行；近零方差传感器 1、5、6、10、16、18、19；工况组合数 FD001 为 1，FD002 和 FD004 为 6。全部对上。

【解释】传感器 6 的标准差是 0.0014。判据如果按原来的 0.001，它就漏掉了，验收就不过。所以判据改成 0.01。这是"先算再说"在代码层面的例子。

【念】s02：两侧区间覆盖率 0.896，q 是 28.8，区间宽 57.7，在 0.87 到 0.93 内，与论文二的流程复现无误。一侧下界，全部循环校准 0.934，一机一点 0.948。RMSE 测试机最后一循环 17.2，论文二报的量级相当，不调参。

【念】s03：三档删失率实际值 0.20、0.40、0.60，与目标差为 0。断言未触发。results/ 出现 15 个 s03 文件。

【停顿】验收全过，不等于结果好看。下面看结果。

---

## 第 17 页　结果一：样本量核算与权重曲线

【念】F04 左图：保留样本占观测样本的比例。删失 20% 时 94% 到 99%，60% 时 79% 到 96%。丢的样本不多，因为丢的只是每台机最后 c0 个循环。

【念】F04 右图是一条水平线：校准折保留发动机在全部 12 个组合里都是 40 台。

【停顿】问学生为什么。答：下线时刻下限 C_MIN = 60，大于最大的 c0 = 50，所以每台机至少有 10 个循环满足 C_i − t ≥ c0。把 C_MIN 调到 20，这条线就会掉下来。

【念】这就是作业二的答案：按"校准折保留发动机"这一列，60% 那档不用放弃。

【念】F05：删失 20% 时权重最大 1.6，接近常数；40% 和 60% 时晚期循环权重顶到 60。60 是下限 1 / 训练折台数触发的结果，不是真实似然比。删失越重、t 越大，留下来的样本要顶越多人的份。这是第 5 页那个发现的真数据版本。

【对照代码】`s03_accounting.csv` 与 `s03_weights.csv`。

---

## 第 18 页　结果二：总体覆盖率与决策区覆盖率随删失率

【承上启下 · 必须说】这是全场最重要的一页结果。先看左图，再看右图，问学生哪个数该报进论文。

【念】左图，总体覆盖率：六条线都在 0.98 到 0.99，名义 0.90。看起来全都达标。

【念】右图，决策区覆盖率，c0 = 30，真实 RUL 小于 30 的 307 个测试点：没有一条线到 0.90。无删失参照 E 只有 0.45。

【停顿】E 都只有 0.45。这意味着什么？

【念】意味着这不是删失处理的问题，是方法框架的问题。y = min(RUL, c0) 之后，13096 个测试点里 12789 个 y = 30，下界封顶后天然覆盖。90% 的保证全花在它们身上，决策区的 307 个点没有自己的保证。

【解释】这不是 bug。这是论文一 Theorem 5 说的条件覆盖不可能，论文二承认的分段问题，在我们这套设定下的具体形态。移交文档写的"已知问题 1"，现在有了真数据的形状。

【承上启下 · 必须说】不要替结果辩护。结果不好看，改的是解释，不是数据。它告诉我们下一步要做什么：决策区需要自己的校准，这就是 Mondrian 那一步存在的理由。

【念】右边的表是六条线在三档删失率下的决策区覆盖率，c0 = 30。Cu 在 40% 时 0.287，Cw 0.388，Cw1 0.487，B 0.528，E 0.450。

【对照代码】`s03_methods.csv` 的 `cov_dec` 列。

---

## 第 19 页　结果三：分段覆盖率，以及覆盖率与紧度的取舍

【念】F08，删失 40%，c0 = 30。[30,60) 和 [60,125] 两段全部是 1.0，这是封顶的必然结果，不是成绩。[0,15) 一段最差，E 是 0.34，Cu 只有 0.10。

【念】Cu 对 Cw，作业三的答案：40% 删失、c0 = 30，决策区覆盖率 0.287 到 0.388；c0 = 50，0.399 到 0.546。只差一个权重。20% 删失时两者几乎没差，因为权重最大才 1.6。第 5 页的发现在真数据上成立，但只在删失重的时候起作用。

【念】F09，横轴平均下界，纵轴决策区覆盖率，右上为好。B 在左上角：覆盖高，紧度差。60% 删失、c0 = 50 时 B 的平均下界 29，E 是 46。

【解释】B 把下线当失效，标签系统性偏低，下界跟着偏低，所以决策区覆盖率最高、平均下界最低。这正是修正三要把平均下界和覆盖率一起报的原因：只看覆盖率，B 是冠军；两个一起看，B 是最保守的那个。

【念】Cw1 比 Cw 覆盖高、下界低，是一机一点的代价；无信息界比例在 60% 时最多 0.9%。

---

## 第 20 页　结果四：一台测试机的轨迹，和校准折的删失时间线

【念】F10，测试发动机 34，删失 40%，c0 = 30。黑线是真实 RUL，彩线是各方法的下界，虚线是 c0。

【念】大部分时间所有下界都贴着 c0 走，这就是"天然覆盖"。真正有信息的只有最后几十个循环：下界开始往下走，能不能走到黑线之下，就是决策区覆盖率在数的东西。B 的线最早往下掉，所以它覆盖高、下界松。

【停顿】让学生指出图上哪一段是决策区。

【念】F03 是第 4 页那张示意图的真数据版：校准折 40 台机按观测长度排序，绿色保留，橙色是每台机最后 30 个循环，实心点失效，空心点删失。

【停顿】让学生数一下：删失机和失效机各丢了多少。答案：都是最后 30 个循环，规则对两类机一样执行。

---

## 第 21 页　结果怎么读：五句话

【承上启下 · 必须说】五句话，第一句最重要，其余四句是它之下的细节。

【念】第一，总体覆盖率不是要看的数。六条线总体 0.96 到 0.99，决策区 0.18 到 0.67。边际保证被远离失效的样本稀释，无删失参照 E 也只有 0.45。这是方法框架层面的问题，优先级最高。

【念】第二，加权有效，但只在删失重的时候。40% 删失时 Cw 比 Cu 决策区覆盖率高 0.10 到 0.15；20% 时权重接近 1，两者没差。

【念】第三，B 的高覆盖是偏保守换来的。覆盖率和紧度必须一起看。

【念】第四，一机一点更稳也更松。Cw1 决策区覆盖率比 Cw 高 0.04 到 0.24，平均下界低 0.2 到 1.7，无信息界最多 0.9%。校准点少的代价。

【念】第五，A 在 60% 时不可信。校准折只剩 16 台失效机，它的数字不进论文。"校准折保留发动机 40 台"这一列没有拦住它，因为拦的是 C 方法。

【停顿】学生若问"那这篇论文还能写吗"。答：能，因为现在知道了要解决什么。E 都不达标，说明决策区需要自己的校准。下次课的 Mondrian 校准就是从这里出发。

【念】以上数字都来自 `results/s03_methods.csv` 与 `logs/s03.txt`，删失为人工构造，Type-I，与寿命独立。

---

## 第 22 页　作业与下次课

【念】作业一：五个脚本跑通，四个 Python 一个 R，控制台输出和图贴到群里，按移交文档第 9 节的顺序。跑不通的贴报错，不要自己改代码。

【念】作业二：看 s03 的核算表，回答 60% 删失率那一档要不要保留。理由只能是"校准折保留发动机"那一列的数。答案第 17 页已经给了，作业是让学生自己从表里找出来，并说清为什么是 40 台。

【念】作业三：对比 s03 结果表里 Cu 和 Cw 的 cov_dec，用一句话解释差从哪来，答案在第 5 页和第 19 页。再对比 Cw 和 Cw1 的 mean_L，说这是谁的代价。

【念】下次课先对作业二、作业三的答案。然后三件事：按预测值分组做 Mondrian 校准，让决策区有自己的保证；把 FD002 与 FD004 合起来用，做依赖工况的删失；把点预测换成分位数回归，做 CQR 版本，看紧度提升多少。c0 怎么选，论文一的做法是在训练折里留一块试。

【念】结束。

---

## 附：全场必须说的六句逻辑扣

时间紧的时候其他都能压，这六句一句不能少。

1. 第 3 页：三处错误的共同点是没算就写。方案里每一个数都要能指到脚本的哪一行。
2. 第 4 页：规则对失效机也执行，不然两群样本差的不只是协变量分布。
3. 第 5 页：完全随机的下线，在样本层面不是完全随机的删失。所以权重不是常数，论文一那句话在这里不成立。
4. 第 7 页：总体覆盖率会被 RUL 远大于 c0 的样本抬高，只有决策区覆盖率是要紧的数。
5. 第 12 页：Cu 对 Cw 只差权重，Cw 对 Cw1 只差校准点，两组差各说明一件事。
6. 第 18 页：无删失参照 E 的决策区覆盖率也只有 0.45，这是方法框架的问题，不是删失处理的问题。不改数据，改解释，下一步是 Mondrian 校准。

## 附：跑出来和预期不符时怎么讲

真实 FD001 上已经跑出来的情况，以及怎么讲：

- 总体 coverage 0.96 到 0.99 而 cov_dec 0.18 到 0.67，E 也只有 0.45：边际保证被远离失效的样本稀释，是已知问题 1，不是 bug。下次课 Mondrian 校准处理。
- 20% 删失时 Cu 与 Cw 几乎没差：F05 显示权重最大 1.6。删失率低、c0 小时权重本来就平。40% 起差距拉开。
- Cw1 的 mean_L 低于 Cw 0.2 到 1.7：一机一点的代价，校准点少分位数粗。trivial 最多 0.9%。
- 校准折保留发动机全是 40 台：因为 C_MIN = 60 > c0。这一列拦不住 A，A 在 60% 时只有 16 台失效机。
- B 覆盖最高、下界最低：标签系统性偏低带来的保守，不是方法好。
- 图是 Linux 预览版（替代字体）。论文用图在 Windows 上重跑 s04，宋体五号加 Times New Roman 五号。
- 任何情况下不改数据、不删结果，改的是解释。
